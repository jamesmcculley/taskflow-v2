# Decisions

- **2026-07-31** — **The review spans a week, a month or a quarter, and highlights roll upward.** The old review was a guided triage walkthrough (Inbox → overdue → projects → Someday), which is hygiene, not reflection — and hygiene the Inbox count already nags you about. What it didn't do is answer "what did I actually get done, and what am I aiming at next", which is the question that gets asked at the end of a month or a quarter and is impossible to answer from memory by then.

  The look-back is built from the **completion log, not live tasks**: work that was finished and then edited, moved, or deleted still happened. It groups by **area, then project**, because that's how a stretch of work is accounted for, and because the grouping still reads months later when the individual titles have gone cold.

  The load-bearing idea is the **star**. Marking a completion as a highlight costs one click in the week you did it, and `selectRolledHighlights` carries it up: a month shows its weeks' highlights, a quarter shows its months' *and* weeks'. So a long look-back is assembled continuously rather than reconstructed at the end from a log nobody can interpret any more. Highlights are stored as **task IDs**, so a highlight follows a completion whose title is later corrected, and dedupes by task when the same item is starred at two levels.

  Roll-up uses **overlap, not containment**: an ISO week straddles a month boundary four times a year, and a week's work doesn't stop counting because the month ticked over mid-week. `src/store/period.ts` is pure UTC date math on ISO strings for the same class of reason — local-time arithmetic shifts a day across a DST boundary, which would move a Sunday completion into the wrong week.

  **Write review note** renders it as plain markdown — ordinary headings, no plugin syntax, nothing that needs TaskFlow installed to read. The file is the artefact, not a view of one, so it can be copied somewhere else wholesale.

- **2026-07-31** — **Cut Stats, Board view, CSV export, and `moveToHeading`.** Surface was the problem: 28 commands and 13 fixed sidebar destinations is more than the plugin earns, and a tool you don't want to open is worse than one missing a feature. Stats (tiles, streaks, heatmap) is habit-tracker gamification rather than task management, and streaks quietly push you to complete things for the graph. Board view is a second rendering of a project that Org's column view would eventually duplicate. CSV export is a once-a-year action carrying a module and a command. `moveToHeading` existed only to serve board drags and had no other caller. Also folded the three `priority-a/b/c` commands into one `cycle-priority` and dropped `schedule-tomorrow`, which the date picker already offers as its second option. 28 commands → 24.

  Kept **Whenever** despite listing it as a cut candidate: it's the only view of undated work inside active projects, and removing it would strand those tasks outside their project page. Its dispatcher entry was mislabelled "All open tasks" — it never showed those, since it requires an active project — and now reads "Unscheduled tasks".

- **2026-07-31** — **Hour repeaters are read but not honoured.** `+1h` parses (a stamp carrying one must still yield its date) but every date in the index is day-granular, so there is nothing for it to advance. It used to shift by zero days, so completing such a task rescheduled it to the same day forever and it never left Today. `advanceDate` now returns null for sub-day units, which routes it into the existing "repeater we can't honour" path: the user gets a Notice and the task completes once. Making it work properly means time-aware recurrence, which is a feature, not a fix.

- **2026-07-31** — **Inbox excludes anything that isn't plain `TODO`.** Giving a task a keyword *is* triage: a NEXT item has been shortlisted and a WAITING one is parked on someone else. Leaving them in Inbox contradicted its own documented rule that processing a task moves it out, and put the one genuinely un-actionable state in the list you work from. Conversely `selectByKeyword` no longer hides someday tasks when a keyword is named — deferring something you're blocked on shouldn't erase the record that someone owes you a reply.

- **2026-07-30** — **ID write-back re-checks the line it is about to write.** A task or checklist item could end up with two `^t-xxxxxx` refs (a bug inherited from v1). `indexFile` queues its write-backs without awaiting them, so two index passes could both read a file whose lines carried no IDs, both decide the same line needed one, and both write. The headline path survived because it re-parsed the block inside `vault.process` and skipped when an ID was already there; the checklist path was v1's unguarded `raw + ' ^' + id` and appended a second one. Confirmed by running the original loop: two passes over `- [ ] Child` produce `- [ ] Child ^t-first ^t-second`. A stale plan could also brand an ID onto an unrelated line, since a sibling write-back (the CLOSED backfill) inserts a line and shifts every index below it — the original loop wrote `CLOSED: [...] ^t-stray`.

  The write loop moved to `src/indexer/ids.ts` as the pure `applyIdAssignments`, where every branch re-checks the line as it stands *now*: skip a line that is no longer a headline or a checkbox, only replace an ID when it is the one the plan meant to replace, and only append when there is nothing there. `queueWrite` additionally chains a file's write-backs so a later write is at least applied to the earlier one's output. Guards are the fix; the chain removes the window they have to catch.

  `parseHeadline` now consumes *every* trailing `^id` rather than the last, so an already-damaged line stops carrying the stray ref in its title and is rewritten with one ID on the next edit. The last ID wins, which is what the old parser already returned — a repaired line therefore indexes as the same task it did while broken, and `data.json` stays consistent.

- **2026-07-29** — Task-level Someday is written as org's **`SOMEDAY` keyword**, not v1's `:someday:` tag. v2 gained the keyword but kept the tag, so one state had two encodings — and `toggleSomeday` wrote the *legacy* one, meaning the plugin's own UI never produced the org-native form. The tag is still read (the indexer, `isSomedayTask`, and `setKeyword` all accept it) so migrated and hand-written vaults keep working, and a toggle now clears it rather than leaving a headline stating the same thing twice. Turning Someday *off* only rewrites the keyword when `SOMEDAY` is the thing doing the work: a `NEXT`/`WAITING` task carrying the old tag keeps its keyword and just loses the tag.

- **2026-07-29** — **Dropped the `:notask:` per-headline exclusion.** It existed because every checkbox in a v1 vault was a task, so opting individual lines out was essential. A v2 task requires an explicit TODO keyword, so `:notask:` could only ever fire on a headline deliberately marked as a task and then deliberately unmarked. The two useful scopes stay: `taskflow: false` frontmatter (real: a template note containing `- TODO {{title}}`) and excluded folders — which matter *more* in v2 than v1, since the indexer can no longer ask the metadataCache which files contain tasks and must `cachedRead` every candidate. The one case this gives up is an example task line inside an otherwise-indexed note; `taskflow: false` covers it at note scope.

- **2026-07-29** — The view type and hover source are **`taskflow-v2-view` / `taskflow-v2`**, not v1's `taskflow-view` / `taskflow`. The fork inherited v1's ids, and Obsidian's `registerView` *throws* on a duplicate view type, so enabling v2 in a vault that already had v1 enabled failed the plugin outright (`Attempting to register an existing view type "taskflow-view"`) — which silently invalidated the whole "both run side by side during migration" premise below. A distinct plugin ID is not sufficient; every id registered into a global Obsidian registry has to be namespaced too. Guarded by `tests/viewIds.test.ts`.

- **2026-07-29** — Every CSS class, CSS custom property, and `@keyframes` name is prefixed **`tf2-`** instead of v1's `taskflow-`. v1 and v2 are designed to run simultaneously during a migration, and Obsidian loads every enabled plugin's `styles.css` into one global stylesheet — v2 originally reused all 89 of v1's selectors, so whichever loaded second won on every shared rule. Keyframe names collided the same way (they're global identifiers, not scoped to a rule). Scoping v2's rules under a root class would *not* have fixed this: v1's `.taskflow-task` would still have matched v2's elements, so the class names themselves had to change. The registry ids above deliberately keep the `taskflow-v2` spelling — they're not CSS.

  The ten shared `is-*` state modifiers (`is-active`, `is-selected`, `is-overdue`, `is-wide`, …) were left unprefixed on purpose. Every rule that uses one is anchored to a `tf2-` subject (`.tf2-task.is-selected`, `.is-wide .tf2-sidebar`), so no v1 rule can match a v2 element or vice versa, and these names intentionally echo Obsidian's own state-class convention. Prefixing them would buy nothing against v1 and would break that alignment.

- **2026-07-27** — **v2 forked from v1** as a sibling plugin (`taskflow-v2`), not a branch: both index the same vault in different syntaxes, and a distinct plugin ID lets them run side by side while a vault is being migrated. A converted task is invisible to v1's tokenizer and an unconverted one is invisible to v2's parser, so they can't fight over the same line.

- **2026-07-27** — Org syntax lives **inside `.md` files**, not in real `.org` files. Obsidian doesn't render, preview, link, or search `.org`, so true org files would make the plugin's own views the only way to read your tasks — losing backlinks, transclusion, graph, and mobile editing. Markdown keeps all of that and Org's syntax survives intact.

- **2026-07-27** — Task headlines are written as **list items** (`- TODO …`) rather than bare `TODO …` lines, even though the latter is what plain Org has. Consecutive unindented lines collapse into a single paragraph in markdown, which would visually run the `SCHEDULED:` planning line onto the headline in reading view; indenting the planning line and drawers under a list item renders them as part of the same item. The parser accepts the bare form (and a leftover `- [ ]` checkbox in front of the keyword) on read, so hand-written or pasted Org still indexes — tolerant read, canonical write.

- **2026-07-27** — The stable task ID defaults to v1's trailing **`^t-xxxxxx` block ref** rather than Org's `:ID:` property, with a setting to switch. The block ref is one token instead of a three-line drawer, and it doubles as a working Obsidian block link (`[[Note#^t-a1b2c3]]`) — moving the ID into a drawer would silently break every such link in the vault. Both forms are always parsed; a drawer `:ID:` wins if a task somehow carries both.

- **2026-07-27** — Every mutation goes through **parse → mutate → re-emit** (`editTaskBlock`) instead of v1's per-token regex line surgery. A v1 task was one line, so patching it in place was reasonable; a v2 task spans a headline, a planning line, and drawers, and the number of regexes needed to patch that safely grows with the cross product of fields. Re-emitting from a parsed object keeps the block canonical for free (Org's `CLOSED`/`DEADLINE`/`SCHEDULED` key order, drawers dropped when empty) and confines every write to the block's own lines. The parsed block deliberately ends at the last *contiguous* drawer: a drawer separated by a blank line, or one that's never closed, is treated as prose and left alone rather than absorbed and rewritten.

- **2026-07-27** — Recurrence moved from free rrule text (`🔁 every week`) to **Org repeaters** (`+1w` / `++1w` / `.+1w`) on the timestamp, which is where Org puts them and which makes the three catch-up semantics explicit — v1's single behaviour was `++`, and "after done" was `.+`. Patterns no repeater can express (nth-weekday-of-month, "every weekday", a fixed weekday) keep their rrule phrase in a `:REPEAT:` property, and the completion path falls back to rrule for exactly those. That fallback is why `rrule` is still a dependency.

- **2026-07-27** — `WAITING` and `SOMEDAY` are todo-*type* keywords, so `Task.status` stays the coarse `todo | done | cancelled` triple every selector already filtered on, and the finer state lives in `Task.keyword`. This kept the entire selector layer, filter layer, and store portable from v1 unchanged; only the sort comparator learned about keywords (NEXT above plain TODOs, WAITING below).

- **2026-07-27** — A nested `- [ ]` checkbox is a **checklist item**; a nested keyword headline is its **own task**. v1 had to infer this from Obsidian's `listItems` parent chain because everything was a checkbox and the two cases were genuinely ambiguous. In v2 the syntax itself distinguishes them, which both matches Org (a subtree carrying its own TODO is a real TODO) and removes a whole class of "is this a task or a sub-item" edge cases.

- **2026-07-27** — The indexer can no longer use the metadataCache to skip files (`listItems[].task` only knows about checkboxes, and keyword headlines aren't cached), so it reads every candidate file through `cachedRead` behind a cheap regex pre-filter. `cachedRead` is served from memory, so this costs a regex per file rather than disk I/O. ID write-back now runs **bottom-up** within a file, because adding an `:ID:` property grows a block and would shift every line number below it.

- **2026-07-27** — Migration **rewrites in place after backing up**, rather than emitting converted copies to a side folder. Tasks are spread across a whole vault and are linked from daily notes and other pages; writing copies elsewhere would leave every link pointing at the unconverted originals and make the user reconcile two trees by hand. The dry run is a separate command that writes nothing and reports every planned diff, so the destructive step is always opt-in and always previewed. Backups are copies into `TaskFlow v1 backup <timestamp>/` inside the vault (not moves), so a failed run leaves the originals in place too. The converter re-runs *inside* `vault.process()` rather than writing the content it read during planning, so an edit made between the scan and the write is converted rather than clobbered.

- **2026-07-27** — The migration deliberately **preserves task IDs and leaves checklist items untouched**, which is what makes `data.json` portable without any data migration at all: manual sort order, `completedAt`, the History log, and saved filters are all keyed by ID. It's also idempotent — already-converted headlines are skipped — so an interrupted run is safe to resume, and a partly-converted vault converges.

- **2026-07-27** — v1's tokenizer is vendored into `src/migrate/v1Tokenizer.ts` verbatim rather than imported or reimplemented, so the migrator reads old files exactly the way the plugin that wrote them did, and v2's own parser never has to carry emoji-era syntax.

- **2026-07-27** — Tag and property constants live in `src/org/tags.ts`, not in the indexer. `convert.ts` needs them and the indexer imports `obsidian`; keeping the constants in a pure module is what lets the converter (and the whole `src/org/` layer) be bundled and unit-tested without mocks — the repo's existing "pure modules have no obsidian imports" rule, applied to a new dependency edge that would otherwise have broken it.

- **2026-07-27** — The agenda repeats a task across days on purpose: a past `SCHEDULED` item carries forward until done (Org's sticky behaviour, and what stops an overdue task from vanishing), a `DEADLINE` appears from its warning window onward, and a task with both stamps lists twice on one day, once per reason. Keyboard navigation collapses those duplicates to distinct tasks so ↑/↓ doesn't visit the same task twice.

- **2026-07-20** — Fixed two real drift sources between History and the daily-note journal, both found by tracing every code path that removes a log entry against every path that's supposed to remove the matching journal line: (1) `removeLogEntry` (History → "Remove from History") never called `dailySync.remove()` at all — a manual removal left the journal line behind forever; (2) `reconcilePersisted`'s automatic pruning (a task reverted to todo outside the plugin — e.g. a native checkbox uncheck, the same class of gap `findUnloggedCompletions` closed for the completing direction) dropped log entries silently, with nothing telling the daily note to catch up. Both now diff the log before/after and call `dailySync.remove()` for every dropped 'done' entry. Added a one-time repair command (`Clean up daily-note lines with no matching History entry`) since fixing the code paths only prevents *new* drift — anything that already accumulated in a vault stays until swept. The scanner matches TaskFlow's own journal-line shape (`- ✅ HH:mm … %%id%%`) specifically rather than any bare `%%...%%`, since that's Obsidian's general-purpose comment syntax and a loose match would risk eating an unrelated note.

- **2026-07-20** — Quick audit pass: re-read the indexer's new completion-detection code (unlogged/drift) for races between the un-awaited `assignBlockIds`/`recordUnloggedCompletions`/`applyStampDrift` calls that can fire back-to-back on the same file. Concluded this is safe on the strength of Obsidian's own documented guarantee for `vault.process()` — "will not run concurrently for the same file" — which the plugin guidelines page corroborates ("atomic, non-conflicting changes"); noted as an assumption to revisit if it ever proves wrong in practice, since I can't verify Obsidian's internals directly. Found and fixed one real gap: the board's dimmed Someday column looked like a drop target but silently no-opped on drop (guarded out since dimmed columns aren't real heading groups) — now drops there call `toggleSomeday` instead of `moveToHeading`, safe unconditionally since any card draggable into it is guaranteed not already someday.

- **2026-07-20** — Closed the remaining half of "edit a done date via markdown": v0.13.0's `findUnloggedCompletions` only caught completions with *no* log entry yet. A new `findStampDrift` handles the other case — an *already-logged* completion whose ✅ stamp gets hand-edited afterward — by comparing the log entry's day against the line's current stamp and, on mismatch, calling the same `editCompletionDate` the "Edit date…" UI uses (so the daily journal moves too, not just the log). Deliberately skipped when a task has more than one 'done' entry (recurring tasks accumulate one per past occurrence) since there's no way to know which historical entry a single current stamp corresponds to — that disambiguated case is what the per-entry "Edit date…" action is for.

- **2026-07-20** — Fixed a real bug (found by auditing a live vault): only the plugin's own actions ever wrote to the completion log, so a task checked off via Obsidian's *native* checkbox (or hand-typed `[x]`, or a change synced in from another device) never got a ✅ stamp or a History entry — it just silently vanished into "done, but never logged." The indexer now runs a `findUnloggedCompletions` pass on every file it indexes: any done/cancelled task with no matching log entry gets one, using its own ✅ stamp date if present (so pre-existing old completions keep their real date) or today's date as a last resort (batched into the same kind of write `assignBlockIds` already does for missing block IDs). This also runs during the first full scan, so pre-existing `[x]` tasks in a vault picking up the plugin for the first time get backfilled into History too — deliberately consistent with how missing block IDs already get silently backfilled on first scan, not something to gate behind a setting. Recurring tasks are *not* auto-advanced by this path (only the plugin's own `completeTask` knows how); a natively-checked recurring task is logged and stamped like any other done task, but its line and dates are left exactly as found. `recordCompletion`'s parameter loosened from `Task` to `Pick<Task, 'id'|'title'|'project'>` so both the plugin's own actions and this passive detection path can share it via a new public `recordExternalCompletion` wrapper.

- **2026-07-20** — History entries can have their completion date corrected via **Edit date…** (right-click), identified the same way `removeLogEntry` already does (taskId + that entry's completedAt — not just taskId, since a recurring task's ID repeats across many historical log entries). If the entry is still the task's *live* completion (line still carries that ✅ stamp), the stamp is rewritten too; if it's a historical entry whose line has since moved on (recurrence rewrote it to the next occurrence), only the log and daily-note journal move to the corrected day — there's no live stamp to touch. `DailySync.record` was refactored to take raw fields (taskId/title/projectPath) instead of a live `Task`, since correcting a historical entry has no live Task to hand it.

- **2026-07-20** — Every task row now shows a source-note chip (`#Note Name`, derived from `task.file`, not written to markdown) so tasks in mixed-origin views (Inbox, Upcoming, Whenever…) show where they live without opening them. Replaces the old project-only chip; project note tasks show the same chip since `project.name` already equals the file's basename. Hidden (`hideSource`) inside a note's own project/area view, where it would just repeat the page you're on. Styled tag-like (accent color, `#` prefix) but kept visually distinct (italic) from real `#tags` so it's never mistaken for something you typed or something clickable-as-a-filter.
- **2026-07-20** — Inbox redefined as a triage holding area, not "wherever a non-project task happens to live": `selectInboxTasks` now also excludes tasks with a scheduled or due date. Scheduling a task moves it out of Inbox (into Today/Upcoming) without moving the file; clearing the date moves it back automatically, so a task can never get silently lost by half-editing it. This only changes the selector — no markdown or file-location logic changed — and the review-mode Overdue step no longer double-lists an unfiled overdue task under Inbox too, since it now belongs only to Overdue.

- **2026-07-20** — `taskflow: false` frontmatter now also accepts the quoted string form (`taskflow: "false"`, any case, plus `"ignore"`/`"off"`/`"no"`). Obsidian's property UI writes a quoted string when the field is typed as a "Text" property, which YAML parses as a string rather than a boolean — the original check only matched the boolean, so a note excluded through the property UI silently stayed indexed. Found via a real vault note ("Test Note.md") where this happened.

- **2026-07-20** — Checkbox exclusion at three scopes: `#notask` tag per line, `taskflow: false|ignore|off` frontmatter per note, and an excluded-folders setting (prefix match on path segments; changing it triggers a full rescan that also evicts newly excluded files from the index). Exclusion happens before ID assignment, so excluded checkboxes never get `^t-` refs appended.

- **2026-07-19** — M7: priority is a standalone `!!`/`!!!` token (a trailing "!" in a title never counts); priority sorts above manual order, and a scheduled time (optional `HH:mm` after ⏳'s date) sorts within the day — compared by codepoint, not localeCompare, whose collation broke the untimed-last sentinel. Capture's recurrence extraction bounds the phrase with a vocabulary whitelist before rrule validates it, because fromText silently ignores trailing junk. Backdated completions timestamp at noon local on the chosen day. Board view is wide-layout only; dragging between columns rewrites the task under the target heading (the no-heading column inserts above the first heading).

- **2026-07-19** — M6: task-level Someday is a plain `#someday` tag (typeable anywhere, incl. mobile) rather than a new emoji token; it's hidden from tag chips and unions with someday-project membership. Tonight is a `🌙` flag; enabling it also schedules today when the task isn't already due/scheduled today. Checklist items are checkboxes nested under a checkbox (through any bullet levels, attached to the root task); they still get block IDs but are no longer independent tasks — they don't appear in lists or project progress. After-completion repeats use the `after done` / `after completion` suffix on the rrule text. Selection moved from React context into the zustand store so Quick search can navigate-and-select.

- **2026-07-19** — Daily-note journal lines are plain `- ✅ HH:mm …` list items, not checkboxes, so the indexer never re-ingests them as tasks; a hidden `%%taskId%%` comment makes removal on uncomplete exact. Recurring tasks completed twice a day produce two lines; removal takes the last. Daily sync is on by default (the feature was requested as a default workflow) with a settings toggle.
- **2026-07-19** — Pinned filters match open tasks only and AND their criteria; tags use prefix matching for nested tags (`dev` matches `dev/backend`). Filters live in data.json (index-owned UI state, like sort order) — not in markdown.

- **2026-07-19** — M4: single click now selects a task; opening the note moved to double-click/Enter. Manual drag-reorder writes list-scoped integer order values to the index only — markdown line order is untouched, so reordering never causes file churn. Reordering is enabled in manually-ordered lists (Inbox, Today, Whenever, Someday, project sections) and not in date-driven Upcoming. Completion linger is 1.6s.

- **2026-07-19** — The indexer reconciles the History with markdown on every (re)index: completions undone outside the plugin (native checkbox in the note, manual edit) are pruned, and closed non-recurring tasks keep only their latest entry. Recurring tasks are exempt (log is their only history). Orphaned entries for deleted lines are kept as history but removable via right-click → Remove from History.

- **2026-07-18** — `every Nth <weekday>` recurrence shorthand is normalized to rrule's `every month on the Nth <weekday>` (monthly semantics, the common expectation); rrule's own parser rejects the short form. Unparseable recurrence text falls back to a plain completion with a Notice rather than blocking the checkbox. Recurrence text must start with `every` — rrule's fromText silently returns a yearly rule for arbitrary junk.
- **2026-07-18** — Next occurrence = first strictly after max(anchor, today), with dtstart at the anchor: overdue repeats skip into the future but stay aligned to the original pattern, and completing early still advances past a future anchor.
- **2026-07-18** — The History reads only the index completion log (per spec); tasks checked off by hand in markdown don't appear there. Uncompleting removes the task's most recent log entry. Recurring completions exist only in the log, since the line is rewritten as the next occurrence.
- **2026-07-18** — Whenever includes tasks that have a due date but no scheduled date (spec says "no scheduled date"; a deadline alone doesn't make a task "scheduled").
- **2026-07-18** — Upcoming/Today placement uses the earlier of scheduled/due (same effective-date rule the M2 tabs used). Upcoming labels: Tomorrow, weekday names through the end of the current Monday-start week, then `Jul 25` (with year if different).
- **2026-07-18** — Narrow/wide layout switches at 480px container width via ResizeObserver: narrow = nav behind a menu toggle, wide = two-pane.

- **2026-07-18** — Plan adjusted: the panel should provide a complete, polished sidebar UI (see ROADMAP.md). Consequences: the M2 This Week/Future tabs are removed in M3 (Upcoming covers both, date-grouped); Areas join the sidebar via `area: <name>` project frontmatter; a new M4 milestone owns visual fidelity and interactions (animations, magic plus, keyboard nav, drag reorder).

- **2026-07-18** — Added "This Week" and "Future" tabs (user request, ahead of M3's Upcoming). Definitions: placement date = the earlier of scheduled/due; This Week = dated after today through Sunday (Monday-start weeks); Future = dated beyond that. Today is excluded (it has its own tab) and undated tasks are excluded (they become Whenever/Someday in M3). M3's date-grouped Upcoming still lands as specced.
- **2026-07-18** — Mutations live in a `TaskActions` module on the plugin rather than inside the zustand store: the store stays a pure data container (unit-testable without Obsidian), while actions need vault access. Views still funnel every mutation through this one module.
- **2026-07-18** — `moveToProject` inserts into the destination before deleting from the source; a crash in between leaves a duplicate rather than a lost task. Moved tasks are normalized to top-level items, and nested child items do not travel with a moved parent (revisit if checklists become first-class).
- **2026-07-18** — Cancelling writes no ✅ stamp to the line (cancellations aren't dated); the timestamp is recorded index-only so the History can group cancelled items by day.
- **2026-07-18** — Editor commands ("… task under cursor") are the "sensible default" targets for hotkey-bindable mutations; sidebar tasks use the context menu and checkbox instead.
- **2026-07-18** — Capture parse precedence: `!due <date>` first, then `>Project`, then `#tags`, then the first remaining natural-language date (chrono, forward-biased) becomes the scheduled date. Project names resolve exact → prefix → substring, falling back to Inbox with a note in the preview.

- **2026-07-18** — Unknown checkbox statuses (e.g. `- [/]`, `- [>]` from other plugins/themes) are indexed as `todo` rather than skipped, so tasks never silently disappear from views. Only `x`/`X` map to done and `-` to cancelled.
- **2026-07-18** — If a task line already carries any block reference (`^whatever`), it is reused as the stable ID instead of appending a second `^t-xxxxxx` — a markdown line can only have one block ref, and existing refs are already stable.
- **2026-07-18** — Tasks in ordinary notes (no `type: project` frontmatter, not `Inbox.md`, not a daily note) are treated as Inbox items; the spec only distinguishes project notes from everything else, and `project: undefined` already means Inbox in the data model.
- **2026-07-18** — Recurrence text (`🔁 …`) is captured up to the next metadata emoji, `#tag`, or trailing block ref, since rrule text never contains those tokens.
- **2026-07-18** — Tokenizer and store selectors are pure modules with zero `obsidian` imports so unit tests need no API mocking; only the indexer/view layers touch the Obsidian API.
- **2026-07-18** — Daily notes are detected by `YYYY-MM-DD` basenames (the Obsidian default); the Daily Notes core-plugin format setting can be wired in later if needed.
