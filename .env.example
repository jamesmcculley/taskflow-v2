# Absolute or repo-relative path to the Obsidian vault used for manual testing.
# `npm run link` symlinks the build output into this vault's plugins folder.
# `npm run seed` copies test-vault/seed/ into this vault.
TEST_VAULT_PATH=./test-vault
